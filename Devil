-- 🔒 WhiteList
local whitelist = {
    ["bestwars17"] = true,
    ["208482724jf"] = true,
    ["ZD_SD17"] = true
}

-- 🌐 Webhook
local webhookUrl = "https://discord.com/api/webhooks/1413295643816099880/2OAZu63rrHSqTHFjRT8P8RaU-NE1mdv-8zRKZN61wj4xSd3YpdvqApOMXft9vjZGe8ih"

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

-- ⚠️ تحقق من WhiteList
if not whitelist[LocalPlayer.Name] then
    LocalPlayer:Kick("🚫 No estás en la WhiteList para usar este script.")
    return
end

-- 🧾 معلومات اللاعب واللعبة
local displayName = LocalPlayer.DisplayName or "Unknown"
local username = LocalPlayer.Name or "Unknown"
local userId = LocalPlayer.UserId or 0
local gameInfo = MarketplaceService:GetProductInfo(game.PlaceId)
local gameName = gameInfo and gameInfo.Name or "Unknown Game"
local gameId = tostring(game.PlaceId)
local executorName = "Delta 1.0.695.956"
local timeCheck = os.date("Today at %I:%M %p")

-- 📨 Embed للويب هوك
local embed = {
    ["title"] = "📢 تم تشغيل السكربت من قبل مستخدم جديد",
    ["color"] = 16711680,
    ["fields"] = {
        {["name"] = "💎 Display Name", ["value"] = displayName, ["inline"] = true},
        {["name"] = "👤 Username", ["value"] = username, ["inline"] = true},
        {["name"] = "🆔 Player ID", ["value"] = tostring(userId), ["inline"] = false},
        {["name"] = "🎮 Game Name", ["value"] = gameName, ["inline"] = false},
        {["name"] = "🗺️ Game ID", ["value"] = gameId, ["inline"] = false},
        {["name"] = "💻 Executor", ["value"] = executorName, ["inline"] = false},
        {["name"] = "⏰ Time Check", ["value"] = timeCheck, ["inline"] = false}
    }
}

-- 🔗 إرسال Webhook
local data = HttpService:JSONEncode({embeds = {embed}})
local requestFunc = request or http_request or (syn and syn.request) or http.request
if requestFunc then
    pcall(function()
        requestFunc({
            Url = webhookUrl,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = data
        })
    end)
end

-- ⚙️ Auto Rebirth مرة وحدة
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local rEvents = ReplicatedStorage:FindFirstChild("rEvents")

if not rEvents then
    warn("❌ لم يتم العثور على مجلد rEvents في ReplicatedStorage")
    return
end

local rebirthRemote = rEvents:FindFirstChild("rebirthRemote")

if not rebirthRemote then
    warn("❌ لم يتم العثور على rebirthRemote داخل rEvents")
    return
end

-- 🌀 محاولة كل الطرق الممكنة لإرسال الريبرث
pcall(function()
    if rebirthRemote:IsA("RemoteEvent") then
        rebirthRemote:FireServer("rebirthRequest")
    elseif rebirthRemote:IsA("RemoteFunction") then
        rebirthRemote:InvokeServer("rebirthRequest")
    elseif typeof(rebirthRemote.FireServer) == "function" then
        rebirthRemote:FireServer("rebirthRequest")
    elseif typeof(rebirthRemote.InvokeServer) == "function" then
        rebirthRemote:InvokeServer("rebirthRequest")
    elseif typeof(rebirthRemote.Fire) == "function" then
        rebirthRemote:Fire("rebirthRequest")
    elseif typeof(rebirthRemote.Invoke) == "function" then
        rebirthRemote:Invoke("rebirthRequest")
    else
        warn("⚠️ لم يتم التعرف على نوع الريموت.")
    end
end)

print("✅ تم تنفيذ Rebirth مرة واحدة بنجاح.")
